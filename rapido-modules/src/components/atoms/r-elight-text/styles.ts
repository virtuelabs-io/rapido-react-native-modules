import { StyleSheet } from 'react-native';
import Utils from '../../../commons/utils/index';

export const Styles = StyleSheet.create({
    eLightText: {
        fontFamily: Utils.getLightFontFamily(),
        fontSize: 18
    }
});

export default Styles
